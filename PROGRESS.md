# 진행 상황과 다음 할 일

마지막 갱신: 2026-08-13 · 브랜치 `feat/retirement-simulation-engine` · 테스트 **553개 통과**

> **이어서 하실 분께** — 커밋되지 않은 작업은 없고 원격까지 push 되어 있습니다.
>
> **LLM 키·외부 데이터 없이 만들 수 있는 MVP 는 여기서 끝났습니다.** 기획서 9개
> 기능과 MVP 추가 기능 5개, 에이전트 A1~A7 이 전부 구현되어 화면에서 동작합니다.
> 남은 것은 [C1~C7 데이터 원문 확인](#3-데이터-원문-확인--코드로-해결되지-않음-️)과
> [W8 안정화·발표](#4-안정화--발표-원-계획서-w8)뿐이고, 앞의 것은 **코드로 해결되지
> 않습니다.**

관련 문서: [`README.md`](README.md) (구현 설명) · [`docs/roadmap.md`](docs/roadmap.md) (축과 전체 계획)

---

## 이 서비스의 축

> **남이 하는 말이 나에게 얼마인지 계산해서,
> 대부분의 경우 "그건 당신에게 중요하지 않습니다"라고 정직하게 말하는 것.**

만든 기능은 개별 항목이 아니라 전부 같은 축의 사례다.

| 입력 | 세상이 하는 말 | 이 서비스가 하는 말 |
|---|---|---|
| 사기 문자 | "제도 바뀌니 지금 갈아타세요" | 연 **9만 9,000원**이 상한입니다 |
| 뉴스 | "ISA 세제 개편" | **발표(1/5단계)**입니다. 고객님껜 연 **2만 6,706원** |
| 진단 결과 | "69세 고갈" | 월 **113만원** 줄이면 95세까지 갑니다 |
| 안정도 점수 | "98점 양호" | 여유는 **월 5만원**뿐입니다 |
| 퇴직금 | "세금 떼면 얼마 안 남아요" | 근속 32년이면 실효 **1.54%**, 308만원입니다 |
| 예금 갈아타기 | "금리 높은 데로 옮기세요" | 이자 **347만원** 더 받는 순간 건보료가 생깁니다 |
| 국민연금 추가납부 | "더 내면 더 받습니다" | 월 40만원 받으실 분껜 **800만원 손해**입니다 |
| 상담 AI 답변 | (그럴듯한 숫자) | 계산에 없는 숫자면 **그 답변을 내보내지 않습니다** |

---

## 완료

### 원 계획서 W1~W7

| 단계 | 내용 | 커밋 |
|---|---|---|
| W1 | 계산 엔진 (자산지도 + 캐시플로우) | `7517291` |
| W2 | 몬테카를로 · 시나리오 · 안정도 점수 · Streamlit | `88c3c06` |
| W3–4 | LangGraph 대화형 프로파일링 + 설명 에이전트 | `ae0d044` `f864b8a` |
| W5 | 금융사기·불완전판매 탐지 + Supervisor | `e10f266` |
| W6 | FastAPI + 시니어 접근성 웹 | `abb85e4` `28e7f2f` |
| W7 | 정책 영향 분석 + 확정여부 표시 (④·⑧) | `0a02dd1` |

기획서 9개 기능(①~⑨) 중 ⑨ 능동알림을 뺀 전부가 구현된 상태.

### 축을 세우면서 추가한 것

| 기능 | 핵심 결과 | 커밋 |
|---|---|---|
| **처방 엔진** | "월 113만원 줄이면 95세까지" · 연금 연기가 오히려 불리(69→67세) | `cf5f31a` |
| **민감도 분석** | 두 인물 모두 **월 생활비**가 결과를 지배 | `9a19d7f` |
| **에이전트 A1** | `LLMClient.converse()` — 3사 도구 사용 (mock 포함) | `c649fdf` |
| **에이전트 A2** | `agents/tools.py` — 계산 엔진을 도구로 노출 | `55776ac` |
| **에이전트 A3** | 실행 루프 · 반복 상한 · fallback · 실행 기록 | `a7a4cc1` |
| **일회성 큰 지출** | 결혼자금 5,000만원 → 고갈 69→**66세** | `4d430e0` |
| **퇴직금 수령 방식** | 근속 32년이면 실효 **1.54%** · 납부 시점까지 비교 | `92b4d54` |
| **건강보험 절벽** | 지금 0원, **2031년(만 65세)부터** 발생 | `c4e2936` |
| **국민연금 추가납부** | 순 4,049만원 이득 · 다만 절벽이 **3년 앞당겨지는** 인물도 | `23a9dd4` |
| **대화 슬롯 보강** | 대화만으로 신규 기능 두 개에 도달 (급여는 묻지 않고 유도) | `60a020e` |
| **에이전트 A7** | 계산에 없는 숫자면 **답변을 버린다** | `edf2c79` |
| **에이전트 A5** | Supervisor 가 분류만 하던 것을 실제로 배달 | `414be54` |
| **에이전트 A6** | 결과를 두고 이어서 묻는 화면 (웹 + Streamlit) | `7bea38f` |
| **에이전트 A4** | 어떤 도구를 어떤 인자로 불렀는지 화면에 노출 | `a7ef4ac` |
| **가족 공유 리포트** | 기한 있는 것 / 천천히 / **안 해도 되는 것** 세 칸 | `25041fb` |

### 에이전트가 드디어 화면에서 쓰인다

A1~A3 에서 도구를 쥔 상담 에이전트를 만들어 두고도 **어떤 화면에서도 쓰지 않고
있었다.** `Advisor` 클래스로만 존재했고 사용자가 "생활비 50만원 줄이면요?"를
물어볼 입구가 없었다. A4~A7 이 그것을 끝냈다.

```
사용자 질문 → Supervisor 분류 → Advisor 가 도구 선택 → core/ 계산
           → 답변의 숫자를 도구 출력과 대조 → 통과한 것만 화면으로
           → 무엇을 어떻게 계산했는지 함께 표시
```

- **A5** 대화의 창구는 하나다. 같은 엔드포인트인데 대화가 끝난 뒤라면 상담으로,
  붙여넣은 문자면 사기 확인으로 간다. 결과가 나온 뒤의 평범한 질문을 상담으로
  넘기는 갈래가 없으면, 대화를 마친 사용자가 무슨 말을 해도 아무 일도 안 일어난다
- **A6** 대화가 끝나도 입력창을 닫지 않는다. 닫으면 에이전트에 닿을 길이 없다.
  예시 질문 세 개를 눌러볼 수 있게 뒀다 — 빈 입력창 앞에서 멈추는 것이 가장 흔한 이탈
- **A7** 브리핑(`explain.py`)은 표시만 하지만 상담 답변은 **막는다.** 브리핑은 화면의
  계산 결과와 대조할 수 있지만 상담 답변은 대조할 대상이 화면에 없다. 한 번은
  스스로 고칠 기회를 주고, 그래도 남으면 계산된 값만으로 만든 문장으로 대체한다
- **A4** 심사 포인트. 기본은 접어 두고 "눌러서 확인" — 시니어에게 먼저 보여야
  하는 것은 답이다

### 작업하다 발견해서 함께 고친 것

계획에 없던 것들이다. 화면에 기능을 붙이자 그제야 보였다.

| 발견 | 왜 문제였나 | 커밋 |
|---|---|---|
| 대화가 신규 기능에 닿지 않음 | `slots.py` 가 가입월수도 퇴직 전 급여도 묻지 않아, **데모 주경로인 대화로는** 국민연금·건보 임의계속가입이 늘 "알려주시면"만 나왔다 | `60a020e` |
| "이 결과 믿어도 되나요?" 가 사기 확인으로 샘 | `믿어도` 가 사기 확인 표현과 그대로 겹쳤다. 내 계산을 묻는 말에 엉뚱한 위험도 판정이 돌아왔다 | `a7ef4ac` |
| mock 이 언제나 첫 도구를 부름 | 지금까지는 테스트에서만 보였지만, trace 를 화면에 띄우자 "국민연금 더 낼까요?"에 `simulate_plan` 이 찍혔다 | `a7ef4ac` |
| mock 상담 답변이 원시 JSON | 대화창에 중괄호가 뜨면 서비스가 고장난 것처럼 보인다 | `414be54` |

### 마지막 세 건 요약

**퇴직금 일시금 vs 연금 (기능 ⑤)** — `92b4d54`
- 퇴직소득세만 **실효세율 근사가 아니라 제도 구조 그대로** 계산 (근속연수공제 → 환산급여 → 연분연승). 퇴직금 2억 기준 근속 5년 3,570만원 / 32년 **308만원(1.54%)** / 40년 193만원
- 세액뿐 아니라 **납부 시점**까지 비교 — 세금을 `LifeEvent` 로 표현해 엔진 수정 없이 반영
- **근속연수를 모르면 계산하지 않고 물어본다** (5년과 40년은 세금 18배 차이)
- 부수 수확: mock 추출기가 `"2억, 32년 다녔습니다"` 의 32를 금액에 삼켜 **2억 32원**이 되던 버그 발견·수정

**건강보험 피부양자 절벽 (B2)** — `c4e2936` · `core/health_insurance.py`
- **계단이 핵심.** 금융소득 연 1,000만원 **이하면 합산에서 빠지고, 1원 넘으면 전액**이 들어간다. 데모 인물은 이자·배당 **347만원(수익률 1.58%p)** 남았다. 1원 차이를 테스트로 고정했다
- **가만히 있어도 다가온다.** 연금은 물가연동으로 오르는데 기준 2,000만원은 고정 → 연도별로 판정해 넘는 해를 찾는다. 데모 인물은 **지금 0원, 2031년(만 65세)부터**
- 임의계속가입(최대 36개월)은 **지역가입자보다 쌀 때만**, 퇴직 직후에만 열리는 창이라 지나간 개월 수로 닫는다
- **재산분 보험료는 넣지 않았다** — 점수표가 없어 근사식을 지어내지 않았다(주택연금과 같은 결정). 이 값이 **하한**이라는 것과 오차의 방향을 화면에 함께 낸다
- 기본 엔진이 이미 얹는 건보료 근사는 `tax_model` 자리로 빼고 돌린다 — **두 번 세지 않기 위해서**
- 곁다리: Streamlit 폼에 근속연수가 빠져 퇴직금 화면이 늘 '모릅니다' 경로였던 것 수정

**국민연금 임의계속가입·추납 (기능 4)** — `23a9dd4` · `core/national_pension.py`
- **또 계단이다.** 가입 119개월이면 노령연금이 평생 0원(반환일시금)이고 120개월이면
  죽을 때까지 나온다. 비례식으로 이으면 "119개월도 절반은 받는다"가 된다. 1개월 차이를
  테스트로 고정했다
- **값어치는 연금 증가액이 아니라 상충에 있다.** 연금을 늘리면 공적연금소득이 피부양자
  판정에 **전액** 들어가 절벽이 앞당겨진다. 수단마다 `늘어난 연금 - 낸 돈 - 추가 건보료`를
  같은 기준(퇴직 시점 화폐)으로 낸다. 데모 인물은 순 **4,049만원 이득**이지만, 연금
  140만원인 사람은 절벽이 **2034 → 2031년**으로 3년 당겨진다
- **이득이 없으면 없다고 말한다.** 월 40만원 받는 사람은 2,665만원을 내고도 **800만원
  손해** — 화면의 '가장 나은 선택'이 "지금 그대로"가 된다
- **임의계속가입 창은 연금을 받는 순간 닫힌다.** 상한은 65세지만 개시가 64세면 48개월,
  조기수령을 고르면 0개월이다. 제도 제약을 코드에 넣었다
- **A값·B값을 지어내지 않았다.** 공단 예상연금액을 기준점으로 삼고 가입기간 계수(별표 1)의
  비율만 쓴다. 그래서 가입월수를 모르면 계산하지 않는다. 기준소득월액을 0으로 두면 보험료가
  0원이 되어 **공짜처럼 보이므로** 그 경로도 막았다
- `health_insurance._premium_events` 를 공개 함수로 승격 — 두 모듈이 물가 되돌림 규칙을
  각자 구현하면 조용히 어긋난다
- 곁다리: 조사 처리를 `formatting` 으로 뺐다. "둘 다**으로**"가 화면에 나가고 있었다

### mock MVP 이후 붙인 것 (DB · 로컬 모델 · 실제 데이터 준비)

| 기능 | 요지 | 커밋 |
|---|---|---|
| 유저 DB (sqlite) | `storage/` 신설. 세션·프로파일·메시지·도구실행·상담을 남긴다. **L1~L4 를 추측이 아니라 집계로 답하기 위한 재료** | `2413942` 계열 |
| 로컬 LLM (vLLM + Llama 3.1 8B) | `base_url` 만 바꿔 기존 `OpenAIClient` 재사용. 도구용 채팅 템플릿을 지정하지 않으면 모델이 도구 호출을 **텍스트로** 뱉는다 | 〃 |
| 재취업 소득 | "월 200만원 벌면 연금 깎이나요" → **A값+200만원까지는 한 푼도 안 깎인다**. 처방이 요구한 소득과 실제 필요액의 차이도 낸다 | `d60c382` |
| 의료비 충격 | "암 걸리면 수억"이 대부분 사실이 아닌 이유(본인부담상한제). **상한액 표는 첨부파일이라 들고 있지 않고, 모르면 물어본다** | `cafae4d` |
| 은퇴 타임라인 · 능동 알림 | 기한을 조건문이 아니라 **날짜로**. 어디서 어떻게 신청하는지까지 | `1973f52` |
| 숫자 출처 문서 | `docs/sources.md` — 화면에 나가는 모든 숫자의 근거. `tests/test_sources_doc.py` 가 코드와 어긋나면 깨뜨린다 | `8dfd85f` |
| **주택 다운사이징** | 처방 레버에 **총자산의 69%**(부동산)가 빠져 있었다. 차액 2억이 아니라 취득세·중개보수·등기·이사비를 뺀 **1억 9,020만원**이 남는다. 취득세는 지방세법 제11조 원문 대조 — 6~9억은 고정세율이 아니라 계산식 | 이번 |

주택 다운사이징에서 조심한 것: **양도소득세를 넣지 않았다.** 1세대 1주택 비과세 여부가
보유·거주기간에 달려 있어 안 묻고는 모른다. 지방교육세·농어촌특별세도 뺐다. 그래서
여기 거래비용은 **하한**이고, 화면이 그렇게 말한다. 판 돈은 **예금으로만** 굴린다 —
투자 수익률을 얹으면 다운사이징 효과가 부풀려진다.

---

## 다음 할 일

### 1. MVP 기능 — 전부 완료 ✅

| | 기능 | 커밋 |
|---|---|---|
| ~~1~~ | ~~일회성 큰 지출~~ | `4d430e0` |
| ~~2~~ | ~~퇴직금 수령 방식~~ | `92b4d54` |
| ~~3~~ | ~~건강보험 피부양자 절벽~~ | `c4e2936` |
| ~~4~~ | ~~국민연금 임의계속가입 · 추납~~ | `23a9dd4` |
| ~~5~~ | ~~가족 공유 리포트~~ | `25041fb` |

### 2. 에이전트 A1~A7 — 전부 완료 ✅

| | 작업 | 커밋 |
|---|---|---|
| ~~A4~~ | ~~trace 를 화면에 노출~~ | `a7ef4ac` |
| ~~A5~~ | ~~Supervisor 에 연결~~ | `414be54` |
| ~~A6~~ | ~~대화 화면을 결과 이후로 확장~~ | `7bea38f` |
| ~~A7~~ | ~~도구 출력에 없는 숫자 차단~~ | `edf2c79` |

### 3. 데이터 원문 확인 — 코드로 해결되지 않음 ⚠️

**남은 작업은 여기부터다.** 아래 항목은 코드가 아니라 원문(법령·공단 공지)을
확인해야 닫힌다.

| | 확인할 것 | 왜 중요한가 |
|---|---|---|
| C1 | `policy_facts.yaml` ISA 파라미터 (기재부 세법개정안) | **"연 9만 9,000원이 상한"** 이라는 핵심 문장이 직접 걸려 있음 |
| C2 | 연기 7.2% / 조기 6% (국민연금법 제61·62조) | 처방 엔진의 연금 레버 |
| C3 | 퇴직소득세 세율표 (소득세법 제48·55조) | **발표에서 이 표를 그대로 띄우게 됨** |
| C4 | 건강보험 요율·기준금액 — 7.09% · 12.95% · 피부양자 2,000만/1,000만 · 재산 5.4억/3.6억 · 최저보험료 19,780원 | 요율은 **매년 바뀐다.** "2031년부터 연 80만원"이 여기 걸려 있음 |
| C5 | 지역가입자 **재산분 점수표** | 확보하면 건보료가 하한이 아니라 실제 금액이 된다. 없으면 근사식을 지어내지 않는다 |
| C6 | 국민연금 **기준소득월액 상·하한** — 617만원 / 39만원 (2024.7~2025.6 적용값) | **매년 7월 조정된다.** 임의계속가입 보험료 "2,665만원"이 여기 걸려 있음 |
| C7 | 추납 상한 119개월 (국민연금법 제92조, 2020.12 개정) · 임의계속가입 65세 (제13조) | 두 상한이 화면의 선택지 개수를 정한다 |

### 4. 안정화 + 발표 (원 계획서 W8)

- **D2** 예외 fallback 점검 — 세션 소멸, 네트워크 끊김, TTS 미지원
- **D3** 발표 자료 — 아키텍처, 의도적 이탈, 안 만든 것과 이유
- **D4** 버퍼 (반드시 남길 것)

> D1(데모 스크립트)은 **하지 않기로 했다.** 필요해지면 그때 만든다.

### 5. LLM·데이터를 연결한 뒤에 볼 것

지금까지는 전부 mock 으로 검증했다. 실제 키를 꽂으면 그때 드러날 것들이다.

| | 확인할 것 | 왜 |
|---|---|---|
| L1 | 실제 모델이 A7 에 얼마나 걸리는가 | mock 은 도구 출력을 그대로 옮겨 적어 **한 번도 걸리지 않는다.** 차단이 과한지 모자란지는 실제 모델로만 알 수 있다 |
| L2 | 도구 선택 정확도 | mock 은 낱말 매칭이다(`agents/mocks.py` 의 `TOOL_KEYWORDS`). 실제 모델이 얼마나 다르게 고르는지 비교하면 도구 설명(description)을 다듬을 근거가 된다 |
| L3 | 상담 답변 말투 | 시니어 대상 문장 길이·존댓말이 프롬프트만으로 지켜지는지 |
| L4 | 응답 시간 | 도구를 두세 번 부르면 왕복이 그만큼 늘어난다. `MAX_ROUNDS=4` 가 적정한지 |

---

## 보류 — 축을 바꾸지 않는 것들

발표에서 "왜 안 했나" 질문이 들어올 항목이므로 **의도적 결정임을 남겨 둔다.**

| 항목 | 이유 |
|---|---|
| 주택연금(역모기지) | 레버 하나가 늘 뿐. 공사 **월지급금 공시표**를 확보하면 도구로 붙인다. 산정식을 근사로 지어내지 않는다 |
| 블록 부트스트랩 몬테카를로 | 두 엔진 정합성 테스트("변동성 0이면 결정론적 결과와 일치")를 다시 세워야 함 |
| 인출 순서 최적화 | 엔진을 단일 pool 에서 계좌 단위로 쪼개야 함 |
| 상품 추천·랭킹 | 데이터가 없다. "상품 유형까지만"이 이미 내린 결정 |
| 가구(부부) 단위 | 유족연금까지 들어가면 모델 복잡도 급증 |
| ⑨ 능동 알림 풀버전 | ~~유저 DB 선행~~ → DB 가 생겨 `changes_since()` 로 구현됨. 남은 것은 푸시·이메일 인프라 |
| 결과 화면 우선순위 · UX | 웹 결과 화면이 17개 섹션이다. 다만 **Streamlit 이 아니라 제대로 만들 프런트에서** 다룬다 (사용자 결정) |
| 배우자 소득의 건보 영향 · 연금 개시 손익분기 나이 | 크기가 작다. 다운사이징 다음 후보 |

---

## 지켜온 규칙 (새로 작업할 때 확인)

1. **`core/` 는 LLM 도 UI 도 import 하지 않는다.** 계산은 순수 함수로만
2. **모든 가정값은 `data/*.yaml` 에 출처 주석과 함께.** 코드에 하드코딩 금지
3. **키 없이(mock) 전체 흐름이 돌아야 한다.** CI 와 발표 당일 안전망
4. **금액 포맷은 `core/formatting.py` 한 곳에서만.** 서버가 완성해서 프런트로 보냄
5. **모르면 추측하지 않고 물어본다.** 근속연수·국민연금 가입월수 사례 참조.
   0 으로 채우면 "세금 없음", "보험료 공짜"처럼 **틀린 답이 그럴듯하게** 나간다
6. **한계는 화면에 함께 내보낸다.** 일변량 민감도, IRP 과세이연 미반영 등
7. 회귀 기준: `tests/test_demo_regression.py` 가 깨지면 **테스트가 아니라 변경을 의심**할 것
8. **에이전트 답변의 숫자는 도구 출력에 있는 것만.** 프롬프트 지시가 아니라
   `agents/advisor.py` 가 기계적으로 대조해서 막는다 (A7)
9. **질문을 늘리기 전에 유도할 수 있는지 본다.** 시니어 대상 서비스에서 질문 하나의
   비용이 크다. 퇴직 전 급여는 퇴직금 ÷ 근속연수로 끌어냈다(법정 산식). 단 유도값은
   `assumed_fields` 에 남겨 화면이 '가정한 값'으로 표시하게 한다

## 실행

```bash
/scratch/hpc201a02/.conda/envs/finagent/bin/python -m pytest      # 662개
uvicorn web.api:app --host 0.0.0.0 --port 8000                     # 웹
streamlit run app/streamlit_app.py                                 # 백업 데모
```
